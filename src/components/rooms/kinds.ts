/** The three looks the owner is trying. Kept apart from the 3D code so the page can list them without loading three.js. */
export type RoomKind = "carousel" | "gallery" | "floating";

export const ROOM_KINDS: RoomKind[] = ["carousel", "gallery", "floating"];
